

const initialState = {
    fields: []

}
const drawField = (state = initialState, action) => {
    switch (action.type) {




        default:
            return state
    }
}
export default drawField;